/**
 * Shared channel filtering configuration for analytics queries
 */

// Default allowed channel categories for analytics
export const ALLOWED_CATEGORIES: string[] = [
  "Need Help",
  "React General",
  "Advanced Topics",
];

// Default allowed channels (currently empty but can be configured)
export const ALLOWED_CHANNELS: string[] = [];
